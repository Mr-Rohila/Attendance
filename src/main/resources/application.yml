server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/attendance
    username: root
    password: system
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
  application:
    name: Attendance-Service
    
attendance:
  monthly:
    csv:
      header: Sno, Employee Id, Employee Name, Month Name 
    